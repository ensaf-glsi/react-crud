import type { NextPage } from "next";
import { CardType } from "types/models";


const Home: NextPage = () => {
  return (
    <div className="max-w-9xl mx-auto px-4 sm:px-6 lg:px-8">
      <div className="max-w-5xl mx-auto">
        Empty Next.js project with tailwindcss
      </div>
    </div>
  );
};

export default Home;
